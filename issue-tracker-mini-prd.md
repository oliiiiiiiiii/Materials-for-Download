# 個人開發者 Issue Tracker｜迷你 PRD

## 產品問題

程式開發人員在管理 Issue 時，需要能快速且一目瞭然地掌握目前有哪些 Issue，以及每一筆 Issue 所處的狀態。本產品第一版以單一列表集中呈現 Issue，並以狀態標籤降低掌握工作進度的成本。

## 目標使用者

- 程式開發人員
- 使用情境：個人管理開發工作中的 Issue

## 核心流程

1. 使用者開啟產品，從單一列表查看所有 Issue。
2. 列表直接顯示標題、狀態、優先級與截止日期；描述不在列表直接顯示。
3. 使用者可搜尋標題，或依狀態、優先級篩選 Issue。
4. 使用者可切換排序規則，或採用手動排序。
5. 使用者透過彈出視窗新增或編輯 Issue。
6. 使用者可直接在列表中切換 Issue 狀態。
7. 使用者刪除 Issue 時，需經二次確認；確認後永久移除。

## 功能需求

### Issue 管理

- 支援新增、編輯及刪除 Issue。
- Issue 欄位：
  - 標題：必填。
  - 描述：選填。
  - 狀態：必填；固定選項為「待處理」、「進行中」、「已完成」。
  - 優先級：必填；固定選項為 1、2、3、4、5，其中 1 最高、5 最低。
  - 截止日期：必填。
- 新增與編輯使用彈出視窗。
- 狀態可直接在列表中切換。
- 刪除前顯示二次確認，確認後永久移除。

### 列表、搜尋、篩選與排序

- 首頁採單一列表，並以狀態標籤呈現 Issue 狀態。
- 每列直接顯示標題、狀態、優先級與截止日期。
- 搜尋僅比對標題。
- 篩選條件僅包含狀態與優先級。
- 預設依截止日期由近到遠排序。
- 使用者可改用：
  - 優先級由先到後（1 → 5）。
  - 優先級由後到先（5 → 1）。
  - 截止日期由近到遠。
  - 截止日期由遠到近。
  - 手動排序。

## 非功能需求

- 資料僅保存在目前裝置的瀏覽器中，不需帳號登入或跨裝置同步。
- 重新整理頁面或關閉後重新開啟瀏覽器，Issue 資料、所選排序方式及手動排序結果均須保留。
- 列表中的狀態需可透過狀態標籤清楚辨識。

## 驗收條件

- 使用者可建立包含必填標題、狀態、優先級與截止日期的 Issue，描述可留空；儲存後該 Issue 出現在列表中。
- 缺少任一必填欄位時，Issue 不可成功建立或儲存。
- 使用者可透過彈出視窗修改 Issue，儲存後列表呈現更新內容。
- 使用者可在列表直接將 Issue 切換為「待處理」、「進行中」或「已完成」。
- 使用者發起刪除時會看到二次確認；取消時資料保留，確認時 Issue 永久移除。
- 列表每列可直接看到標題、狀態標籤、優先級與截止日期，且不直接顯示描述。
- 輸入標題關鍵字後，只顯示標題符合的 Issue；描述中的相同文字不影響搜尋結果。
- 使用者可依狀態或優先級篩選列表，結果只包含符合條件的 Issue。
- 首次使用或尚未另選排序方式時，列表依截止日期由近到遠排列。
- 五種排序方式皆可選用，並依所選規則正確排列；手動排序模式下可調整 Issue 順序。
- 重新整理頁面及關閉後重新開啟瀏覽器，Issue、排序方式與手動排序結果保持不變。

## 第一版不做事項

- 帳號登入。
- 雲端儲存與跨裝置同步。
- 搜尋 Issue 描述。
- 依截止日期篩選。
- 列表直接顯示描述。
- 刪除後復原。

## 待確認問題

- 產品支援的裝置類型、瀏覽器及最低版本。
- 空列表、搜尋無結果與篩選無結果時的呈現方式。
- 新增 Issue 時，狀態、優先級與截止日期是否需要預設值。
- 標題與描述的字數上限，以及是否需要其他格式驗證。
- 截止日期是否包含時間、採用哪個時區，以及逾期 Issue 的呈現方式。
- 多個 Issue 具有相同截止日期或優先級時的次要排序規則。
- 手動排序的操作方式，以及切換到手動排序時採用目前順序或既有手動順序。
- 搜尋與篩選是否可同時使用，以及多個篩選值之間採用的組合規則。
- 編輯彈出視窗如何被開啟，以及關閉未儲存內容時是否提醒。
- 刪除確認視窗需呈現的資訊與確認文案。
- 瀏覽器資料被清除、無痕模式或儲存空間不足時的處理方式。
- 無障礙、效能、資料容量與離線使用要求。
- 是否需要匯入、匯出、備份或還原資料。
- 除已明確排除項目外，第一版是否還有其他不做事項。
